<UserControl x:Class="WpfApp1.Views.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Banner Area -->
        <Grid Grid.Row="0">
            <!-- Simulated Banner Background -->
            <Border Background="#FF1A1A1D">
                <Grid>
                    <Rectangle Opacity="0.4">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF3A1C71" Offset="0" />
                                <GradientStop Color="#FFD76D77" Offset="0.5" />
                                <GradientStop Color="#FFFFA07A" Offset="1" />
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                    
                    <!-- Content Over Banner -->
                    <StackPanel VerticalAlignment="Bottom" Margin="40,0,0,60">
                        <Border Background="{StaticResource AccentBrush}" HorizontalAlignment="Left" Padding="10,4" CornerRadius="2" Margin="0,0,0,10">
                            <TextBlock Text="NEW SEASON" FontWeight="Bold" FontSize="12" Foreground="Black" />
                        </Border>
                        <TextBlock Text="VOLCANIC PEAKS" FontSize="64" FontWeight="Black" Foreground="White" />
                        <TextBlock Text="Explore the new fiery depths and master the elements in the biggest update yet." 
                                   Style="{StaticResource BodyText}" FontSize="18" MaxWidth="600" Opacity="0.8" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom Actions Bar -->
        <Border Grid.Row="1" Height="140" Background="{StaticResource SidebarBackgroundBrush}" BorderBrush="#1AFFFFFF" BorderThickness="0,1,0,0">
            <Grid Margin="40,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Status Information -->
                <StackPanel VerticalAlignment="Center" Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <Ellipse Width="10" Height="10" Fill="Lime" Margin="0,0,10,0" />
                        <TextBlock Text="SERVERS ONLINE" FontSize="12" FontWeight="Bold" Foreground="Lime" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource CaptionText}" />
                </StackPanel>

                <!-- Play Button Section -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="PLAY" Style="{StaticResource PlayButtonStyle}" Command="{Binding PlayCommand}" Width="240" Height="60" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="WpfApp1.Views.Pages.NewsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="LATEST NEWS" Style="{StaticResource TitleText}" Margin="0,0,0,30" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding AllNews}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource NewsCardStyle}" Width="300" Height="400">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="180" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <!-- Placeholder Image -->
                                <Border Grid.Row="0" Background="#FF2A2A2E" CornerRadius="8,8,0,0">
                                    <Rectangle Fill="{StaticResource AccentBrush}" Opacity="0.1" />
                                </Border>

                                <StackPanel Grid.Row="1" Margin="20">
                                    <TextBlock Text="{Binding Category}" Style="{StaticResource CaptionText}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource SubtitleText}" TextWrapping="Wrap" Margin="0,5,0,10" />
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource BodyText}" TextWrapping="Wrap" Opacity="0.6" MaxHeight="80" />
                                    
                                    <TextBlock Text="{Binding Date, StringFormat={}{0:MMMM dd, yyyy}}" 
                                               Style="{StaticResource CaptionText}" VerticalAlignment="Bottom" Margin="0,20,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
